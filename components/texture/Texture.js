import React from "react";
import classes from "./texture.module.scss";

export default function Texture() {
  return (
    <div className={classes.bg}>
      <div className={classes.texture}></div>
    </div>
  );
}
